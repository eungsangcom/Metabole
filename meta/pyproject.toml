[project]
name = "meta"
version = "0.1.0"
description = "Metabole — Hexagonal + Clean + DDD modular monolith"
requires-python = ">=3.11"
readme = "../README.md"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["apps"]
pythonpath = ["apps"]

[tool.ruff]
line-length = 100
target-version = "py311"
